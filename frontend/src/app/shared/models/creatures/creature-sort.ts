import {SortType} from '../../../core/components/sorts/sort-type.enum';
import {SortValue} from '../../../core/components/sorts/sort-value';

export class CreatureSort {
  sortType: SortType;
  sortValues: SortValue[] = [];
}
